Input diff (summary): added one formatKST() function to utils/date.ts + one test.
Intent declared: "Add a date format util — utils/date.ts and the test only."
Gate verdict:
  SECRET-SCAN PASS / SCOPE PASS (within declared scope) / DEPENDENCY PASS (no new package)
  / CODE-DIFF PASS (no debug/conflict markers)
Overall: PASS → proceed to commit.
Explanation: scope matches the declaration and there are no risky patterns, so it passes. The gate did not change the code.
